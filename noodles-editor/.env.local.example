# Needed for Tile3DLayer and Directions Op (optional)
VITE_GOOGLE_MAPS_API_KEY=          # Get token at https://developers.google.com/maps/documentation/javascript/get-api-key

# Needed for Tile3DLayer (Cesium ion) (optional)
VITE_CESIUM_ACCESS_TOKEN=          # Get token at https://cesium.com/ion/tokens

# Needed for Geocoder and driving directions. Optional if not using these features.
VITE_MAPBOX_ACCESS_TOKEN=          # Get token at https://account.mapbox.com/access-tokens/

# Needed for MapTiler base maps (optional)
VITE_MAPTILER_API_KEY=             # Get token at https://cloud.maptiler.com/account/keys/

# Claude AI API Key (optional)
# If not set here, the app will prompt you to enter it in the UI
# Your API key is stored locally in the browser and never sent to Noodles.gl servers
VITE_CLAUDE_API_KEY=               # Get token at https://console.anthropic.com/

# PostHog Analytics (optional)
# Privacy-preserving product analytics for understanding feature usage
# Users can opt-out in settings
VITE_POSTHOG_API_KEY=              # Get project key at https://app.posthog.com/project/settings
VITE_POSTHOG_HOST=                 # Optional, defaults to https://app.posthog.com
