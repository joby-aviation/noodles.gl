{
  "instructions": "Answer each question about THIS repository. Write answers.json at the repository root: a JSON object mapping each question id to a concise answer string (one or two sentences; lists where asked). Base every answer on what is actually in this repository.",
  "questions": [
    { "id": "q01", "kind": "field-schema", "question": "List TripsLayerOp's input fields and the default value of each (name the field, then its default; fields with no default can say 'none')." },
    { "id": "q02", "kind": "field-schema", "question": "What is the default value of ScatterplotLayerOp's getRadius input?" },
    { "id": "q03", "kind": "field-schema", "question": "What is the default value of ScatterplotLayerOp's radiusUnits input?" },
    { "id": "q04", "kind": "field-schema", "question": "Which values does ScatterplotLayerOp's radiusUnits input accept?" },
    { "id": "q05", "kind": "field-schema", "question": "What is the name of ScatterplotLayerOp's output field?" },
    { "id": "q06", "kind": "field-schema", "question": "Which values does FileOp's format input accept?" },
    { "id": "q07", "kind": "field-schema", "question": "What is the default value of FileOp's format input?" },
    { "id": "q08", "kind": "field-schema", "question": "What are the names of AccessorOp's input field and its output field?" },
    { "id": "q09", "kind": "field-schema", "question": "What is the name of MaplibreBasemapOp's output field?" },
    { "id": "q10", "kind": "field-schema", "question": "What is the name of DeckRendererOp's output field?" },
    { "id": "q11", "kind": "field-schema", "question": "What is the default value of TripsLayerOp's trailLength input?" },
    { "id": "q12", "kind": "enum-semantics", "question": "Does MathOp's operator input support 'sqrt'? Does it support 'atan'?" },
    { "id": "q13", "kind": "enum-semantics", "question": "Which values does TripsLayerOp's widthUnits input accept?" },
    { "id": "q14", "kind": "enum-semantics", "question": "Which values does MaplibreBasemapOp's projection input accept?" },
    { "id": "q15", "kind": "enum-semantics", "question": "Inside an AccessorOp expression, which variables are available, and what does each refer to?" },
    { "id": "q16", "kind": "operator-history", "question": "What happened to MapStyleOp, and how are projects that used it handled?" },
    { "id": "q17", "kind": "operator-history", "question": "What exactly did schema migration 006 ('rename nodes to noodles') rename?" },
    { "id": "q18", "kind": "operator-history", "question": "Which operator was renamed in schema migration 008, and to what?" },
    { "id": "q19", "kind": "operator-history", "question": "What did schema migration 013 add to project files?" },
    { "id": "q20", "kind": "format-invariant", "question": "What must an edge's id look like in a noodles.json project file? Give the exact formula." },
    { "id": "q21", "kind": "format-invariant", "question": "Which prefix do input (target) handles carry in project-file edges?" },
    { "id": "q22", "kind": "format-invariant", "question": "Which prefix do output (source) handles carry in project-file edges?" },
    { "id": "q23", "kind": "format-invariant", "question": "What is the current noodles.json project schema version number?" },
    { "id": "q24", "kind": "format-invariant", "question": "In a FileOp url like '@/data.csv', what does the '@/' prefix mean?" },
    { "id": "q25", "kind": "format-invariant", "question": "What format do node ids follow in a noodles.json project file?" },
    { "id": "q26", "kind": "format-invariant", "question": "In a CodeField expression, how do you reference the 'data' output of an operator at path /data-loader? Give the exact expression." },
    { "id": "q27", "kind": "format-invariant", "question": "In an operator reference, what does the relative path op('./sibling') resolve to?" },
    { "id": "q28", "kind": "format-invariant", "question": "How is an operator nested inside a container addressed? Give an example path." },
    { "id": "q29", "kind": "format-invariant", "question": "How many connections can a single operator input accept at once?" }
  ]
}
