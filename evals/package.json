{
  "name": "noodles-evals",
  "private": true,
  "type": "module",
  "description": "CUJ eval harness (dev-docs/specs/agent-ready-docs/07-cuj-evals.md). Runs against the repo, not inside the app — plain Node only, never imports Vite-built code.",
  "scripts": {
    "run": "tsx harness/run.ts",
    "grade": "tsx harness/grade.ts",
    "report": "tsx harness/report.ts",
    "selftest": "tsx harness/selftest.ts",
    "calibrate": "tsx harness/calibrate.ts"
  },
  "dependencies": {
    "@anthropic-ai/bedrock-sdk": "^0.32.0",
    "@types/node": "^26.1.0",
    "@types/pngjs": "^6.0.5",
    "pngjs": "^7.0.0",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
