name = "noodles-gl"

# Cloudflare Pages configuration
pages_build_output_dir = "website/build"

[build]
command = "yarn install:all && yarn build:all"

# Compatibility settings
compatibility_date = "2024-01-01"

# Build configuration
[build.upload]
format = "service-worker"

# Environment variables (add any you need here)
# [vars]
# NODE_ENV = "production"
